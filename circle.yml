machine:
  node:
    version: $(cat $HOME/$CIRCLE_PROJECT_REPONAME/usabilla.js/.nvmrc)

dependencies:
  cache_directories:
    - ~/.cache/yarn
  override:
    - yarn install

test:
  override:
    - yarn test
